# Repo metadata to set on GitHub (copy-paste)

Set these once when you create the public repo. They drive how the repo reads at a glance and how it surfaces in search.

## About (top-right "About" panel)

**Description:**

```
Durable execution for TypeScript AI agents. Crash-proof workflows that resume exactly where they left off. Zero cloud, zero cost, zero dependencies.
```

**Website:**

```
https://www.npmjs.com/package/@sudhanshu1402/keel
```

(Check "Use your GitHub Pages website" off; point Website at the npm page until a docs site exists.)

## Topics (Settings -> add topics, or the gear in the About panel)

```
durable-execution
workflow-engine
ai-agents
llm
typescript
nodejs
ollama
local-first
resilience
retry
crash-recovery
zero-dependency
```

## Social preview (Settings -> Social preview)

Upload a 1280x640 card. Reuse the tokyonight style from `meta/social-cards`. The card text should be the one-line pitch above plus the name `keel`.

## Repository name

`keel` (namespaced as `sudhanshu1402/keel`, so no collision with the npm `keel` package, which is why npm publishing uses the scope `@sudhanshu1402/keel`).
